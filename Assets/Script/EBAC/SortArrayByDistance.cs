using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

public class SortArrayByDistance : MonoBehaviour
{
    /*
    public List<Coins> itens = new List<Coins>();

    void RegisterCoins(Coins i)
    {
        if(!itens.Contains(i))
            itens.Add(i);
    }

    void Sort()
    {
        itens = itens.OrderBy(x => Vector3.Distance(this.transform.position, x.transform.position)).ToList();
    }
    */
}
